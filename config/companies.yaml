regions:
  - "Germany"
  - "DE"

companies:
  - name: NVIDIA
    ats: workday
    config:
      base_url: "https://nvidia.wd5.myworkdayjobs.com"
      site_path: "/wday/cxs/nvidia/NVIDIAExternalCareerSite"
      site_name: "NVIDIAExternalCareerSite"
      # Optional: filter by search text or facets
      # search_text: "software engineer"
      # applied_facets:
      #   jobFamilyGroup:
      #     - "0c40f6bd1d8f10ae43ffaefd46dc7e78"  # Engineering

  - name: Waymo
    ats: greenhouse
    config:
      board_slug: "waymo"

  - name: Wayve
    ats: greenhouse
    config:
      board_slug: "wayve"

  - name: Google
    ats: google
    config: {}

  - name: Mercedes-Benz
    ats: mercedesbenz
    config: {}

  - name: Volkswagen
    ats: volkswagen
    config: {}

  - name: BMW Group
    ats: bmw
    # NOTE: bmwgroup.jobs is protected by Akamai bot detection that blocks cloud provider
    # IP ranges (AWS/Azure/GCP). Live scans work from residential/self-hosted IPs but will
    # return 0 jobs when run from GitHub Actions. To enable in CI, use a self-hosted runner.
    config: {}
